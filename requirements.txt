# ---------------------------------------------------------------------------
# Core pipeline requirements.
#
# These install cleanly on CPU-only machines (including Intel macOS) and are
# enough to run: data curation, benchmark construction, evaluation metrics,
# LLM-as-judge, the A/B inference server and report generation.
#
# For GPU training (QLoRA on a 7B/8B base model) additionally install
# requirements-gpu.txt on a CUDA host.
# ---------------------------------------------------------------------------

# --- config / plumbing ---
pyyaml>=6.0.1
pydantic>=2.7
typer>=0.12
rich>=13.7
python-dotenv>=1.0.1
tqdm>=4.66

# --- data ---
datasets>=2.19
pandas>=2.2
numpy>=1.26,<2  # torch wheels below are built against the NumPy 1.x ABI

# --- modelling (CPU wheels are fine; CUDA hosts get the cu12 build) ---
# transformers/peft/trl/accelerate are pinned, not just floored: newer TRL
# replaced the completion-only-loss collator this project relies on with a
# mechanism that silently no-ops for Mistral (see src/coderefine/train.py's
# module docstring). This exact combo is the one actually verified end-to-end.
# torch's floor is lowered from 2.4, not pinned: newer transformers requires
# torch>=2.5, which has no Intel-macOS wheel at all (PyPI dropped x86_64
# macOS builds after torch 2.2.2) — but a CUDA host should still get whatever
# recent torch build it already has, so only the floor changes, no ceiling.
torch>=2.2
transformers==4.46.3
peft==0.13.2
trl==0.11.4
accelerate==1.2.1
tokenizers<0.21
sentencepiece>=0.2
safetensors>=0.4.3

# --- evaluation ---
sacrebleu>=2.4
Levenshtein>=0.25
scipy>=1.13

# --- figures ---
matplotlib>=3.8

# --- experiment tracking ---
wandb>=0.17
mlflow>=2.14

# --- serving ---
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# --- LLM-as-judge providers (either one is optional at runtime) ---
anthropic>=0.34
openai>=1.40

# --- tests ---
pytest>=8.2
